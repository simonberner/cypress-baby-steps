{
  "name": "cypress-baby-steps",
  "version": "1.0.0",
  "description": "first steps with cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:test": "cypress run"
  },
  "author": "Simon Berner",
  "license": "ISC",
  "devDependencies": {
    "@bahmutov/cy-api": "^1.4.2",
    "cypress": "^4.5.0",
    "cypress-cucumber-preprocessor": "^2.3.1"
  },
  "dependencies": {
    "typescript": "^3.9.2"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
